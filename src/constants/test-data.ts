/**
 * Known fixtures from the live Toolshop catalog, named so specs read as intent
 * instead of carrying magic strings. If the catalog renames an item, change it here.
 */
export const Products = {
  pliers: 'Pliers',
} as const;
